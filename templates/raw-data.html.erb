---
layout: page
title: Raw Data
permalink: /data/
note: DO NOT EDIT THIS FILE. It is generated from templates/raw-data-html.erb
---
<p>There are multiple contributions from the same institution in some of the categories below. This is because the data was tracked multiple times. In some cases data may have been tracked for multiple projects, or processes may have been timed while being performed by different staff or on different types of material.</p>
<p>
  Detailed information about each contribution is also available. Use the contribution ID on this page to locate additional information about individual submissions in the <a href="https://docs.google.com/spreadsheets/d/15-cF2Gn17t48_9DZ8NT0pKm1AC8VwKCXq3_kUwAjbfQ/edit?usp=sharing">contributions spreadsheet</a>.
</p>

<h2>Image Capture Stastics</h2>
<table class="table">
  <tr>
    <th>Image Capture Device Type</th>
    <th>Average</th>
    <th>Min</th>
    <th>Max</th>
    <th>Samples</th>
    <!-- <th>
      Median
    </th> -->
  </tr>
  <% @scanner_types.each do |scanner, data| %>
  <tr>
    <td><%= scanner %></td>
    <td><%= "%0.1f" % data['average'] %></td>
    <td><%= data['min'] %></td>
    <td><%= data['max'] %></td>
    <td><%= data['raw_times'].length %></td>
  </tr>
  <% end %>
</table>

<h2>Raw Image Capture Data</h2>
<% @scanner_types.each do |scanner, data| %>
<h3><%= scanner %></h3>
<table class="table">
  <tr>
    <th>ID</th>
    <th>Institution</th>
    <th>Make/Model</th>
    <th>Time per 100 Scans</th>
    <!-- <th>Lookup Row</th> -->
  </tr>

  <% data['raw_times'].each do |value| %>
  <tr>
    <td><%= value[:id] %></td>
    <td><%= value[:institution] %></td>
    <td><%= value[:make] %></td>
    <td><%= value[:time] %></td>
    <!-- <td><%= value[:row] %></td> -->
  </tr>
  <% end %>
</table>
<% end %>
<!-- end scanner -->

<section id="preparation_times">
  <h2>Preparation of Original Materials Times</h2>
  <table class="table">
    <tr>
      <th>Task</th>
      <th>Average</th>
      <th>Min</th>
      <th>Max</th>
      <th>Samples</th>
      <!-- <th>
        Median
      </th> -->
    </tr>
    <% @prep_times.each do |task, data| %>
    <tr>
      <td><%= task.gsub('_', ' ').split.map(&:capitalize).join(' ') %></td>
      <td><%= "%0.1f" % data['average'] %></td>
      <td><%= "%0.1f" % data['min'] %></td>
      <td><%= "%0.1f" % data['max'] %></td>
      <td><%= data['raw_times'].length %></td>
    </tr>
    <% end %>
  </table>

  <% @prep_times.each do |task, data| %>
    <h3><%= task.gsub('_', ' ').split.map(&:capitalize).join(' ') %></h3>

    <table class="table">
      <tr>
        <th>ID</th>
        <th>Institution</th>
        <th>Percentage</th>
        <th>Time per 100 Items</th>
        <th>Normalized Time</th>
        <!-- <th>Lookup Row</th> -->
      </tr>
      <% data['raw_times'].each do |value| %>
      <tr>
        <td><%= value[:id] %></td>
        <td><%= value[:institution] %></td>
        <td><%= "%0.1f" % value[:percentage] %></td>
        <td><%= "%0.1f" % value[:time] %></td>
        <td><%= "%0.1f" % value[:normalized] %></td>
        <!-- <td><%= value[:row] %></td> -->
      </tr>
      <% end %>
    </table>
  <% end %>
</section>

<section id="quality_control">
  <h2>Quality Control Times</h2>
  <table class="table">
    <tr>
      <th>Task</th>
      <th>Average</th>
      <th>Min</th>
      <th>Max</th>
      <th>Samples</th>
      <!-- <th>
        Median
      </th> -->
    </tr>
    <% @quality_control_stats.each do |task, data| %>
    <tr>
      <td><%= task.gsub('_', ' ').split.map(&:capitalize).join(' ') %></td>
      <td><%= "%0.1f" % data['average'] %></td>
      <td><%= "%0.1f" % data['min'] %></td>
      <td><%=  "%0.1f" % data['max'] %></td>
      <td><%= data['raw_times'].length %></td>
    </tr>
    <% end %>
  </table>

  <% @quality_control_stats.each do |task, data| %>
    <h3><%= task.gsub('_', ' ').split.map(&:capitalize).join(' ') %></h3>

    <table class="table">
      <tr>
        <th>
          ID
        </th>
        <th>Institution</th>
        <th>Percentage</th>
        <th>Time per 100 Items</th>
        <th>Normalized Time</th>
        <!-- <th>Lookup Row</th> -->
      </tr>
      <% data['raw_times'].each do |value| %>
      <tr>
        <td><%= value[:id] %></td>
        <td><%= value[:institution] %></td>
        <td><%= "%0.1f" % value[:percentage] %></td>
        <td><%= "%0.1f" % value[:time] %></td>
        <td><%= "%0.1f" % value[:normalized] %></td>
        <!-- <td><%= value[:row] %></td> -->
      </tr>
      <% end %>
    </table>
  <% end %>
</section>

<section id="post_processing">
  <h2>Post Processing</h2>
  <table class="table">
    <tr>
      <th>Task</th>
      <th>Average</th>
      <th>Min</th>
      <th>Max</th>
      <th>Samples</th>
      <!-- <th>
        Median
      </th> -->
    </tr>
    <% @post_processing_times.each do |task, data| %>
    <tr>
      <td><%= task.gsub('_', ' ').split.map(&:capitalize).join(' ') %></td>
      <td><%= "%0.1f" % data['average'] %></td>
      <td><%= "%0.1f" % data['min'] %></td>
      <td><%=  "%0.1f" % data['max'] %></td>
      <td><%= data['raw_times'].length %></td>
    </tr>
    <% end %>
  </table>

  <% @post_processing_times.each do |task, data| %>
    <h3><%= task.gsub('_', ' ').split.map(&:capitalize).join(' ') %></h3>

    <table class="table">
      <tr>
        <th>
          ID
        </th>
        <th>Institution</th>
        <th>Percentage</th>
        <th>Time per 100 Items</th>
        <th>Normalized Time</th>
        <!-- <th>Lookup Row</th> -->
      </tr>
      <% data['raw_times'].each do |value| %>
      <tr>
        <td><%= value[:id] %></td>
        <td><%= value[:institution] %></td>
        <td><%= "%0.1f" % value[:percentage] %></td>
        <td><%= "%0.1f" % value[:time] %></td>
        <td><%= "%0.1f" % value[:normalized] %></td>
        <!-- <td><%= value[:row] %></td> -->
      </tr>
      <% end %>
    </table>
  <% end %>
</section>

<section id="metadata_stats">
  <h2>Metadata Creation</h2>
  <table class="table">
    <tr>
      <th>Task</th>
      <th>Average</th>
      <th>Min</th>
      <th>Max</th>
      <th>Samples</th>
      <!-- <th>
        Median
      </th> -->
    </tr>
    <% @metadata_times.each do |task, data| %>
    <tr>
      <td><%= task.gsub('_', ' ').split.map(&:capitalize).join(' ') %></td>
      <td><%= "%0.1f" % data['average'] %></td>
      <td><%= "%0.1f" % data['min'] %></td>
      <td><%=  "%0.1f" % data['max'] %></td>
      <td><%= data['raw_times'].length %></td>
    </tr>
    <% end %>
  </table>

  <% @metadata_times.each do |task, data| %>
    <h3><%= task.gsub('_', ' ').split.map(&:capitalize).join(' ') %></h3>

    <table class="table">
      <tr>
        <th>ID</th>
        <th>Institution</th>
        <th>Percentage</th>
        <th>Time per 100 Items</th>
        <th>Normalized Time</th>
        <!-- <th>Lookup Row</th> -->
      </tr>
      <% data['raw_times'].each do |value| %>
      <tr>
        <td><%= value[:id] %></td>
        <td><%= value[:institution] %></td>
        <td><%= "%0.1f" % value[:percentage] %></td>
        <td><%= "%0.1f" % value[:time] %></td>
        <td><%= "%0.1f" % value[:normalized] %></td>
        <!-- <td><%= value[:row] %></td> -->
      </tr>
      <% end %>
    </table>
  <% end %>
</section>

<section id="post_preparation_stats">
  <h2>Post Preparation</h2>
  <table class="table">
    <tr>
      <th>Task</th>
      <th>Average</th>
      <th>Min</th>
      <th>Max</th>
      <th>Samples</th>
      <!-- <th>
        Median
      </th> -->
    </tr>
    <% @post_preparation_times.each do |task, data| %>
    <tr>
      <td><%= task.gsub('_', ' ').split.map(&:capitalize).join(' ') %></td>
      <td><%= "%0.1f" % data['average'] %></td>
      <td><%= "%0.1f" % data['min'] %></td>
      <td><%=  "%0.1f" % data['max'] %></td>
      <td><%= data['raw_times'].length %></td>
    </tr>
    <% end %>
  </table>

  <% @post_preparation_times.each do |task, data| %>
    <h3><%= task.gsub('_', ' ').split.map(&:capitalize).join(' ') %></h3>

    <table class="table">
      <tr>
        <th>ID</th>
        <th>Institution</th>
        <th>Percentage</th>
        <th>Time per 100 Items</th>
        <th>Normalized Time</th>
        <!-- <th>Lookup Row</th> -->
      </tr>
      <% data['raw_times'].each do |value| %>
      <tr>
        <td><%= value[:id] %></td>
        <td><%= value[:institution] %></td>
        <td><%= "%0.1f" % value[:percentage] %></td>
        <td><%= "%0.1f" % value[:time] %></td>
        <td><%= "%0.1f" % value[:normalized] %></td>
        <!-- <td><%= value[:row] %></td> -->
      </tr>
      <% end %>
    </table>
  <% end %>
</section>
